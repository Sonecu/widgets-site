machine:
  environment:
    CIRCLE_BUILD_DIR: $HOME/$CIRCLE_PROJECT_REPONAME
    PATH: $PATH:$CIRCLE_BUILD_DIR/bin
  post:
    - mkdir -p $CIRCLE_BUILD_DIR/bin

dependencies:
  pre:
    - go get -v github.com/bep/s3deploy
  cache_directories:
    - bin

deployment:
  s3deploy:
    branch: master
    commands:
      - s3deploy -source=public/ -bucket=$BUCKET
